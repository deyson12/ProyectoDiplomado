<%= form_for(level_doctor) do |f| %>
  <% if level_doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(level_doctor.errors.count, "error") %> prohibited this level_doctor from being saved:</h2>

      <ul>
      <% level_doctor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
